@page "/date-binding"

<p>
    <label>
        <code>yyyy-MM-dd</code> format:
        <input @bind="startDate" @bind:format="yyyy-MM-dd" />
    </label>
</p>

<p>
    <code>startDate</code>: @startDate
</p>

<button @onclick="VerifyValues">Debug</button>

@code {
    private DateTime startDate = new(2020, 1, 1);

    void VerifyValues() {
        Console.WriteLine("hi");
    }
}